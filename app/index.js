/* eslint-disable */
import 'styles/index.scss';
/* eslint-enable */
const win = window;

$(() => {
    var hola = () => {
        return 'hola'
    }
  // On resize window
  $(win).resize(() => {
  })
});
