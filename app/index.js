/* eslint-disable */
import 'styles/index.scss';
/* eslint-enable */

const win = window;
const casa = 'addfas';

$(() => {
    const hola = () => casa;
    hola();
    // On resize window
    $(win).resize(() => {
        //
    });
});
